# Copy this file to .env (git-ignored) and fill in your values.
# The example scripts load it automatically via kaizen.load_env().

# Required for --llm features (Claude-written reflections, Sensei questions,
# fishbone drafts). Get a key at https://platform.claude.com
ANTHROPIC_API_KEY=
